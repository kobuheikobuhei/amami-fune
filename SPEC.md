# 奄美大島 船舶動静ブログ 仕様書

最終更新: 2026-09-04 / 状態: 仕様確定（実装前）

---

## 0. このサイトが何であるか

奄美大島に関係する定期航路の**欠航・臨時便・ダイヤ変更**を、公式発表から自動収集して
最速で掲載するブログ。主な読者は**島民の生活利用者**。

設計上の最優先事項は速報性ではなく**「誤った情報を生活判断に使わせないこと」**。
速さは公開の自動化ではなく監視の粒度と、常設ページによる「今の状態」の一元化で確保する。

---

## 1. 決定事項一覧（Q1〜Q37）

### A. 読者と目的
| # | 項目 | 決定 |
|---|---|---|
| 1 | 主な読者 | 島民の生活利用者 |
| 2 | 運営目的 | まずは無償運営。将来的に収益化（広告枠を確保した設計にする） |
| 3 | 運営者名義 | 屋号（サイト名）のみ ＋ 連絡用メールアドレスを明示 |
| 4 | 更新頻度 | 荒天時に1日何本出てもよい（速報優先） |
| 5 | 言語 | 日本語のみ |

### B. 対象範囲
| # | 項目 | 決定 |
|---|---|---|
| 6 | 対象航路 | 幹線（鹿児島〜奄美〜沖縄）＋ 瀬戸内町営 ＋ 喜界島航路 |
| 7 | 貨物船 | 旅客便と同等に扱う（協同組海運を含む） |
| 8 | クルーズ船 | 名瀬港寄港予定を週次・月次のまとめとして掲載 |
| 9 | 小型船（海上タクシー等） | 一切扱わない |
| 10 | 空路 | 監視しない。各航空会社の運航情報への**リンクのみ**常設 |
| 11 | 過去データ | 収集は初日から常時行う。統計記事化は後日 |
| 12 | Web非公開の航路 | 電話確認はしない。Web・SNSに出た情報のみ掲載 |

### C. 情報源
| # | 項目 | 決定 |
|---|---|---|
| 13 | 一次情報の範囲 | 公式サイト ＋ 公式SNS ＋ 自治体・港湾の公式発表 |
| 13b | 集約サイト | **九州のりものinfo.com は「早期検知用」**。ここでの変化は記事の根拠にせず、検知後に該当社の公式で裏取りしてから公開する |
| 14 | X（旧Twitter） | ブラウザ自動化で公開ページを読む（補助扱い。壊れても運用は止めない） |
| 15 | 気象情報 | 気象庁の公式発表のみ |
| 16 | 読者からの情報提供 | 受け付けるが**そのまま記事化しない**。裏取りキューに入れ、公式で確認できたもののみ掲載 |
| 17 | アクセス間隔 | 平常1時間 / 荒天30分。robots.txt遵守、User-Agentに連絡先明記、1社あたり毎回数リクエストまで |
| 18 | 自分の一次取材 | 当面行わない（自動収集のみ） |

### D. 記事の作り
| # | 項目 | 決定 |
|---|---|---|
| 19 | 記事の粒度 | **二層構成**。航路別の常設ページ（Bloggerのページ）＋ イベントごとの速報記事（投稿） |
| 20 | 続報の扱い | 同一記事を更新し、末尾に更新履歴を追記 |
| 21 | タイトル規則 | `【日付】船社・状態｜区間` 例: `【9/2】マリックスライン 上り便 欠航｜鹿児島〜名瀬` |
| 22 | 平常時の投稿 | しない（常設ページのステータス更新のみ） |
| 23 | ラベル設計 | 航路 ＋ 船社 ＋ 状態 の3軸（1記事あたり3〜4個） |
| 24 | アイキャッチ | 文字だけの自動生成画像。状態で色分け（欠航=赤 / 臨時便=青 / 再開=緑 / 注意=黄） |
| 25 | 引用方針 | 事実（日付・便名・区間・状態）のみ自分の言葉で記述。公式発表文のコピペと時刻表の丸写しはしない。公式リンクを必ず付す |

### E. 公開基盤（Blogger）
| # | 項目 | 決定 |
|---|---|---|
| 26 | 投稿方法 | Blogger API v3（OAuth2）。投稿・ページの作成／更新に使用 |
| 27 | パーマリンク | 日付＋船社＋状態のローマ字を**カスタムパーマリンクで明示指定**<br>例: `/2026/09/20260902-marix-kesshin-kagoshima-naze.html` |
| 28 | 公開ゲート | 公式発表に完全一致するものは**自動公開**（欠航・ダイヤ変更・運航再開・条件付き運航）。運航可否未定のみ下書き＋通知 |
| 29 | 通知先 | メール |
| 30 | SNS自動転送 | 最初は行わない（記事データを構造化保存し、後から追加可能にしておく） |
| 31 | 実行環境 | **クラウドで常時実行**（GitHub Actions） |
| 32 | 月額運用費 | 0円。記事生成は定型文の置換で行い、AIによる推測を挟まない |
| 33 | ドメイン | まずは `xxx.blogspot.com` のまま |

### F. 運用・リスク
| # | 項目 | 決定 |
|---|---|---|
| 34 | 誤報時 | 記事は削除せず、本文先頭に訂正を明記して残す |
| 35 | 取得失敗時 | 該当航路に「取得失敗中（最終取得: ◯時◯分）」を赤字表示 ＋ メール通知 |
| 36 | 掲載停止要請 | 即座に該当航路の掲載を停止（航路単位で無効化できる設定を持つ） |
| 37 | MVP範囲 | 幹線3社の監視 ＋ 常設ページ ＋ 速報投稿 |

### 追加確定事項
- **電話番号は一切掲載しない。** 常設ページ・記事とも案内は公式サイトへのリンクのみ。`sources.yml` にも保持しない
- 情報源に**協同組海運**を含める（正式な社名表記とURLは実装時に公式ページで確認）
- 情報源に**九州のりものinfo.com 鹿児島県ページ**を含める（早期検知用）
  `http://www.norimono-info.com/sp/sp_main?disp=area&pref=kago&lang=`

---

## 2. システム構成

### 2.1 エージェント配置

すべてGitHub Actions上で動作。単一のワークフローを定期起動し、
1回の起動の中で複数周まわす（§2.2）。

**分け方の原則**

- 収集層は**壊れる単位**で分ける。公式サイトの作りが変わったときに直す箇所が
  1つに収まるよう、情報源の系統ごとに1つ置く。会社や島でまとめない
- それ以外は**寿命が違うもの**で分ける。毎回作り直すものと、一度作って
  あとから更新するものを同じ役に持たせない
- 収集層は**文章を書かない**。判断は `curator`、表現は `writer` と
  `page-builder` に集約する

| 層 | エージェント | 役割 | 実装 |
|---|---|---|---|
| 収集 | `watcher-aline` | マルエーフェリー・奄美海運。A"LINE の航路別RSS 2本 | `watchers/aline.js` |
| 収集 | `watcher-marix` | マリックスライン。一覧ページ＋個別ページ（RSSは予備） | `watchers/marix*.js` |
| 収集 | `watcher-kyodogumi` | 共同組海運。HTMLの「船舶動静」欄 | `watchers/kyodogumi.js` |
| 収集 | `watcher-jma` | 気象庁の警報と台風 | `watchers/weather.js` `watchers/typhoon.js` |
| 収集 | `fleet` | 各社の配船予定と時刻表を読み、突き合わせて検査する。**疑わしければ何も返さない** | `fleet/*` `watchers/marix-search.js` |
| 段取り | `scheduler` | モード判定（平常／荒天）と収集の間引き | `scheduler.js` |
| 判断 | `curator` | 差分検出、イベント化、重複判定、続報の紐付け、継続中の案内、通常運航の船 | `curator.js` |
| 表現 | `writer` | 速報記事。題名・URL・本文・ラベル・訂正 | `writer.js` |
| 表現 | `page-builder` | 常設ページ。運航状況・台風情報・航路別 | `dailypage.js` `typhoonpage.js` `statuspage.js` ほか |
| 反映 | `publisher` | Blogger API v3。公開と下書きの振り分け | `publisher.js` |
| 監視 | `auditor` | 日次点検。取得・分類・抽出・矛盾・欠落・未投稿・重複・消失・孤立・陳腐化・配船 | `audit.js` |

`src/main.js` はエージェントではなく実行本体である。上の役を順に呼び、
通知と診断を集めて `state/` へ書き出す。順序そのものが仕様になっている箇所
があるため（案内やページのURLは前の周で記録したものを使う）、ここに判断を
足さないようにする。

計11。当初の8から次のように組み替えた。

**削除**

- `watcher-aggregator`（九州のりものinfo.com）— 二次情報だけでは投稿しない方針
  （`confirmed_at` 必須）なので早期検知の実利が薄い。HTTP専用で構造とrobotsも
  未確認のまま実装していない。`sources.yml` に `role: trigger` として残すが、
  エージェントとしては置かない
- `writer` のアイキャッチ画像生成（Q24）— 未実装。必要になった時点で戻す

**分解**

- `watcher-island` — 「島」という区切りが、壊れる単位と一致していなかった。
  喜界〜知名航路はマルエーと同一サイトのため `watcher-aline` に含まれる。
  共同組海運は別サイトなので独立させた。瀬戸内町営（Xのみで配信）と
  クルーズ寄港（週次まとめ）は未実装でフェーズ3

**統合**

- `watcher-weather` と台風の収集 → `watcher-jma`。同じ提供元で、
  どちらもモード判定と台風ページの材料になる

**新設**

- `fleet` — 当初の表に無かったが、実装の中で最も大きく最も壊れやすい塊。
  「どの船がいつ出るか」を各社の公式から読み、突き合わせて検査する（§8）
- `scheduler` — モード判定と間引きは気象の収集の中に置いていた。
  いつ集めるかは収集の仕事ではないので切り出した。間隔を変えるときに
  公式サイトの読み取りに触らなくて済む

**分離**

- `page-builder` を `writer` から分けた。記事は一度書いて続報で更新するもの、
  常設ページは毎回作り直すもので、寿命が違う

**残っている配置のずれ**

表現が収集層に一部漏れている。読者に見せる語（「ドック入りのため運休」）を
watcher が決め、台帳に焼き込んでいる。そのため語を直しても既存の記事は直らず、
揃えるための道具（`tools/resync-articles.js`）を別に持つことになった。
本来は台帳には公式の原文だけを持ち、語は表現層で組み立てるべきである。


### 2.2 実行スケジュール

**GitHub Actions の定期実行は指定どおりには起動しない。**
15分ごとを指定していたが、実測では1日5〜6回・平均3.7時間おきしか起動せず、
最長7時間28分空いた。1日1回の点検も3時間半遅れていた。頻度の細かい指定ほど
間引かれる。起動そのものは相手任せなので、**起動できた時間を無駄にしない**
作りにする。

- 毎時0分に起動を試みる（粗い指定のほうが通りやすい）
- 起動できたら、**節目の時刻まで待ちながら繰り返す**。持ち時間は5時間
  （`MAX_MINUTES`）。1回の起動で夕方から夜、あるいは朝をまるごと覆う
- 次の節目が持ち時間を超えるとき、または90分より遠いとき（`MAX_WAIT_MINUTES`）は
  終了して実行環境を返す。眠って過ごさないため
- 荒天モードでは10分ごとに節目が立つので、待ちが上限に達せず夜通し見張る。
  平常時の夜は空くが、その時間帯に出る発表はほとんど無い

実測の起動時刻（JST 04:38 / 09:10 / 13:34 / 17:11 / 19:40）で試算すると、
平常時は朝 04:38〜08:41 と夕方から夜 17:11〜22:01 が埋まり、
日中の起動は節目が遠いため即座に終了する。実行環境の使用は1日9〜11時間。
荒天時は各ジョブが5時間走り切り、順番待ちで繋がってほぼ切れ目が無くなる。

**節目の時刻**（`src/scheduler.js` の `checkpoints()`）

機械的な刻みでは、18:00に鹿児島を出た船が次の周まで「次の出港」のまま残る。
意味のある時刻はこちらで分かっているので、そこまで待って起きる。

| 出どころ | 節目 |
|---|---|
| 公式の時刻表（`data/timetable.yml` から導く） | 07:00 那覇港発／08:30 鹿児島着／12:20 平土野港着／12:50 平土野港発／17:30 鹿児島北埠頭発／18:00 鹿児島新港発／19:00 那覇港着 |
| 発表が出やすい時間帯 | 05:00〜08:40 と 16:00〜22:00 を20分ごと |
| 日付の切り替わり | 00:05 |

平常時は1日36回。**時刻表を直せば節目も自動で追従する。**
途中の寄港地は入れない（表示していないため更新する意味がない）。
荒天モードでは、節目の間も10分ごとに見る。

- 日本時間の日付が変わったときは、間隔に関わらず必ず収集する
  （「運航状況」ページは日付をまたいだ瞬間に中身が変わるため）
- `shouldRun()` に残した間隔（5分）は、二重に収集しないための下限だけ。
  いつ収集するかは節目の一覧が決める
- **周ごとに state/ をコミットする。** 途中でジョブが止まっても、
  そこまでの台帳を残す。台帳を失うと同じ欠航を二度投稿する
- 通知は周ごとに上書きされるため、消える前に集めてから1通で送る
- 1回の起動が50分ほど走るため次の起動と重なりうる。重なると同じ台帳を見た
  2つのジョブが二重投稿しかねないので、打ち消さずに順番待ちさせる
- 荒天モードへの昇格: 気象庁が奄美地方に海上警報・波浪警報を発表、または台風接近時
- 平常モードへの復帰: 警報解除後6時間、かつ全航路が平常運航に戻ってから
- 日次 22:30 JST 指定: `auditor` 実行（実測では3時間半ほど遅れて走る）
- 週次 日曜: クルーズ船寄港予定・臨時便のまとめ生成

**まだ解けていないこと**

節目まで待つ仕組みは、ジョブが生きている間しか効かない。**起動が定時になった
わけではない。** 完全に定時にするには、ジョブを長く生かす（1本の上限は6時間）か、
ジョブの最後に次を予約する（`GITHUB_TOKEN` では workflow を起動できないため
個人アクセストークンが必要）かのいずれかが要る。どちらも実行環境の占有か
トークンの管理という代償があるため、まだ入れていない。

**早期検知フロー（集約サイトの使い方）**

`watcher-aggregator` は起動のたびに必ず実行する（1リクエストのみ）。
ここで変化を検知したら、通常のインターバルを待たずに該当社の公式ページを即時取得する。
変化がなければ公式への巡回は通常インターバルのままとする。

これにより、各社公式へのアクセス数を増やさずに検知だけを早められる。
Q17で定めたアクセス方針を守りながら速報性を上げるための中核的な仕組み。

> **GitHub Actionsの定期実行は大きく遅れることがある。**
> 運用開始後の実測で、4時間以上あいた例があった（無料の共有基盤のため混雑時に後回しにされる）。
> 平常時は実害が小さいが、荒天時に同じことが起きれば欠航の掲載が遅れる。
>
> 対策として起動を15分ごとに増やした。遅延しても次の機会が早く巡ってくる。
> 実際に収集を行う間隔は平常1時間・荒天30分のまま変えていない（仕様Q17）。
> 起動しても間隔に達していなければ、その回は何もせず終了する。

### 2.3 状態の定義

各航路・各便が取りうる状態を以下に固定する。これ以外の状態を作らない。

| 状態 | 意味 | 色 | 自動公開 |
|---|---|---|---|
| `normal` | 平常運航 | 灰 | 記事化しない |
| `cancelled` | 欠航 | 赤 | 可 |
| `conditional` | 条件付き運航 | 黄 | 可 |
| `extra` | 臨時便・ダイヤ変更 | 青 | 可 |
| `resumed` | 運航再開 | 緑 | 可 |
| `undecided` | 運航可否未定 | 黄 | 不可（下書き） |
| `unknown` | 取得失敗 | 赤（枠） | 記事化しない。常設ページに警告表示 |

自動公開しないのは `undecided` のみ。

当初は `conditional` も下書きに留める設計だったが、これは誤りだった。
「判断が入るもの」として下書きにすべきなのは、こちらが気象などから推測した情報であって、
**会社が公式に「条件付運航」と発表したものは推測ではなく事実**である。
どの港が条件付きかまで公式が明示しており、それを伝えないことの損失の方が大きい。

`undecided`（運航可否未定）は、発表そのものに中身が乏しく短時間で覆るため下書きに留める。
集約サイトのみで検知して公式で未確認の段階も、公開せず検知フラグとしてのみ扱う。

---

## 3. データ設計

```
船舶動静/
├── SPEC.md                  この文書
├── data/
│   ├── routes.yml           航路・船社・船名マスタ、航路ごとの有効/無効フラグ（Q36用）
│   └── sources.yml          監視先URL・公式X・robots確認日。電話番号は持たない
├── state/
│   ├── mode.json            平常 / 荒天
│   ├── snapshots/           ソース別の前回取得結果（正規化JSON）
│   └── events.jsonl         イベント台帳。重複公開防止と統計の元データ
├── drafts/                  承認待ちの下書き
└── .github/workflows/       定期実行の定義
```

### events.jsonl の主要フィールド
`event_id`（航路＋日付＋便＋状態から生成）/ `route_id` / `operator` / `service_date` /
`status` / `source_url` / `detected_via`（aggregator / official / sns）/ `detected_at` /
`confirmed_at`（公式で裏取りできた時刻）/ `published_at` / `blogger_post_id` /
`permalink` / `confidence`（A: 公式明記 / B: 要判断）/ `revisions[]`

`event_id` が既に台帳にあれば新規投稿せず、`blogger_post_id` の記事を更新して
`revisions[]` に追記する。これがQ20（同一記事更新）の実装根拠になる。

`confirmed_at` が空のイベントは公開しない。集約サイトでの検知だけでは公開に至らない、
というルールをデータ構造で強制する。

---

## 4. 運用ルール

- **免責文を全ページ・全記事に固定表示**。「最終的な運航可否は必ず各社公式サイトでご確認ください」＋公式リンク
- 訂正は本文先頭に赤字で `【訂正】9/3 07:00 当初「全便欠航」と掲載しましたが誤りでした` の形式
- 取得失敗が2回連続したらメール通知。6時間継続したら該当航路を「取得失敗中」表示に切り替え
- 掲載停止要請を受けたら `routes.yml` の該当航路を `enabled: false` にするだけで停止できる
- 読者からの情報提供は裏取りキューに入るのみ。公式で確認できるまで記事にもページにも出さない

---

## 4.1 相手先への負荷（2026-09-11 実測）

自動で取りに行く以上、相手先のサイトに迷惑をかけない形であることを
数字で確かめておく。

**1回の収集**（圧縮された実際の転送量）

| 相手先 | 回数 | 転送量 |
|---|---|---|
| marixline.com（一覧ページ） | 1 | 252 KB |
| aline-ferry.com（RSS 2本） | 2 | 15 KB |
| kyoudougumikaiun.co.jp | 1 | 15 KB |
| jma.go.jp | 2 | 1 KB |

**配船の取り直し**（12時間ごと）

| 相手先 | 回数 | 転送量 |
|---|---|---|
| marixline.com（検索、POST） | 10 | 4.7 MB |
| aline-ferry.com（ページとPDF） | 4 | 0.3 MB |

**1日あたり**（節目36回＋取り直し2回）は全体で約200回・13 MB。
marixline.com への負担が最も大きく、約56回・12 MB。
人が10ページ見て回るのと同程度で、常識的な範囲に収まっている。

**直した2つの取りすぎ**

- マリックスの個別ページは1枚1MBある。船名と条件付きの港だけを使うのに、
  通常運航と過去の便は台帳に載らないため毎回「未知のURL」として
  取り直していた。**1回あたり10 MB**。取得結果を `state/marix-details.json` に
  覚え、過去の便は取りに行かないようにした
- 配船の窓が7日先まであり、検索へ1回18往復・8.4 MB送らせていた。
  表示に使うのは前日から翌日までなので3日先に縮めた

**User-Agent**

素性が分かる形で送る。`AmamiFuneBot/1.0 (+https://amami-fune.blogspot.com)`。
`CONTACT_EMAIL` を設定すれば連絡先も添える。以前は未設定時に
`example.com` を送っており、それでは先方が問い合わせようがなかった。

**robots.txt**

| 相手先 | 内容 | 取得先の可否 |
|---|---|---|
| aline-ferry.com | `/cms/wp-admin/` のみ Disallow | 可 |
| kyoudougumikaiun.co.jp | `/library/` `/material/` を Disallow | 可（`/page1` は対象外） |
| jma.go.jp | robots.txt なし（404） | 制限なし |
| marixline.com | **到達できない** | 制限の宣言なし |

marixline.com は `/robots.txt` が `/robots.txt/` へ、さらに `/robots.txt//` へと
転送され続ける。CMSが末尾へスラッシュを付け足す設定で、robots.txt そのものが
置かれていない。制限は宣言されていないが、明示的な許可でもないため、
負荷はこちらで抑える。Crawl-delay はどの相手先にも指定が無い。

---

## 5. 段階計画

### フェーズ1（済）
幹線3社（マルエーフェリー / マリックスライン / 奄美海運）と共同組海運の監視 →
常設ページ → 速報投稿 → 配船予定にもとづく「現在の運航」まで通した。

共同組海運は当初フェーズ2に置いていたが、公式ページの読み取りが安定したため
フェーズ1へ移した。`watcher-aggregator` による早期検知は置かないことにした（§2.1）。

### フェーズ2（未着手）
公式Xの補助監視。瀬戸内町営（加計呂麻・請・与路）は運航状況がXでのみ
配信されるため、取得可否の検証がそのまま着手条件になる。空路リンク集は設置済み。

### フェーズ3
クルーズ船寄港の週次まとめ、統計記事、SNS転送、独自ドメイン。

---

## 6. 実装前に確認が必要な事項

1. **各社の正式な社名表記・運航状況ページのURL**（協同組海運を含む）
2. **各社サイトのrobots.txtと利用規約** — 自動取得が禁止されていないか。九州のりものinfo.com も同様に確認する
3. **九州のりものinfo.com の取得方式** — 同サイトはHTTP専用の可能性がある。GitHub Actionsからは素のHTTPリクエストで取得できるため実装上の問題はないが、ページ構造（更新時刻の取得方法、事業者ごとの状態表記）を実地で確認する必要がある
4. **マルエーフェリー・奄美海運のBBS形式ページ** — `asp2.mg21.jp` で「運航状況（船舶動静）鹿児島航路」が配信されている。公式サイトより機械的に読みやすい可能性があるため、どちらを主監視先にするか検証する
5. **公式Xの取得可否** — Xは未ログインでの閲覧が制限される場合があり、ブラウザ自動化でも取得できない可能性がある。取得できなければフェーズ2から外す
6. **Blogger API のOAuthリフレッシュトークン取得** — 初回のみ手動。GitHub Secretsに保存
7. **GitHub Actionsの無料枠** — privateリポジトリは月2,000分。荒天時48回/日の実行では超過しうるため、**publicリポジトリでの運用を推奨**（認証情報はSecretsに置き、リポジトリには含めない）

---

## 7. 情報源の検証結果（2026-09-04 実施）

`data/routes.yml` と `data/sources.yml` を作成した際の実地確認の結果。

### 確認できたこと

**マルエーフェリーと奄美海運は同一サイト（A"LINE / aline-ferry.com）で運航状況を配信しており、RSSが取得できる。**
WordPress構成で、航路別・船別にカテゴリが分かれている。

- 鹿児島航路: `https://www.aline-ferry.com/status/status-route/route-kagoshima/feed/`
- 喜界〜知名航路: `https://www.aline-ferry.com/status/status-route/route-amami/feed/`
- 記事URLが `/status/route-kagoshima/ferry-akebono/17809/` の形式で、URLから船を特定できる

**マリックスラインもRSSが取得でき、タイトルの構造が極めて良い。**
`https://marixline.com/service/feed/`

```
2026年9月6日（日） 鹿児島新港発 奄美群島経由 那覇港向け 下り便【臨時便・航行経路変更】
2026年9月6日（日） 鹿児島新港発 奄美群島経由 那覇港向け 下り便【航行経路変更・条件付運航】
2026年9月5日（土） 鹿児島新港発 奄美群島経由 那覇港向け 下り便【欠航】
```

日付・出発港・方向・状態がすべてタイトルに含まれるため、**タイトルのパースだけで状態を判定できる**。
**「運休」と「ドック」は分けて判定する。**
船がドックに入っても、別の船が入れば便は動く。一方「運休となります」と
書かれていればその日は便が無い。共同組海運のドック案内は運休を明記し、
マルエーフェリーのドック案内は船の入渠を告げるだけで運休には触れない。
同じ「ドック」の語でも意味が違うので、運休の有無で先に分ける。

表示する言い回しは、判定した状態と食い違うものを使わない。
ドック入りの案内には運航再開の予定日も併記されるため、
この確認をしないと再開の便にまで「ドック入り」と付けてしまう。

【 】内の語と §2.3 の状態定義の対応は以下とする。

| タイトル中の語 | 状態 |
|---|---|
| 欠航 | `cancelled` |
| 臨時便 | `extra` |
| 条件付運航 | `conditional` |
| 航行経路変更 | `extra`（単独で現れた場合） |
| 通常運航 / 平常運航 | `normal` |

**共同組海運の正式名称は「共同組海運株式会社」**（協同ではなく共同）。
`https://www.kyoudougumikaiun.co.jp/page1` に「船舶動静」欄があり、臨時便・ドック運休が掲載される。
航路はAコース（鹿児島〜名瀬〜徳之島〜喜界島）とBコース（鹿児島〜名瀬〜徳之島〜沖永良部）、船は「みさきⅡ」「つばさ」。

**気象庁の警報JSONが取得できる。** `https://www.jma.go.jp/bosai/warning/data/warning/460100.json`
奄美地方の細分区域コードを含む。警報種別はコード番号で表現されるため、コード対応表の作成が必要。

**robots.txt**
- aline-ferry.com — `/cms/wp-admin/` のみ Disallow。取得に問題なし
- town.setouchi.lg.jp — robots.txt が404（制限なし）
- marixline.com — リダイレクトループで取得できず。実装時に再確認

### 仕様に影響する発見

**瀬戸内町は公式サイトに運航状況を掲載していない。**
`/senpaku/cho/unkojokyo.html` を確認したところ、運航状況は **X（@st_liner_info）と町の防災情報メールでのみ配信**されており、
Webページには状態そのものが載っていない。

これはQ12（Web・SNSに出た情報のみ掲載）とQ14（Xはブラウザ自動化）の判断が、
この航路では**単なる補助ではなく唯一の経路**になることを意味する。
Xが取得できなければ加計呂麻・請・与路の情報は掲載できない。フェーズ2の着手前に取得可否を検証し、
不可の場合は常設ページに「運航状況は町の公式Xでご確認ください」とリンクを置く形に切り替える。

**奄美海運のフェリーあまみが運休中。**
2026-05-16の機関故障により運休し、フェリーきかいが週3便体制（鹿児島発 月・水・金／平土野発 火・木・土）で代替運航中。
平常ダイヤと異なる状態が継続しているため、常設ページに運休と代替体制を明示する必要がある。

### 未検証のまま残っている項目

1. 九州のりものinfo.com のページ構造とrobots.txt（HTTP専用のため手元のツールから取得できず）
2. 公式X（@st_liner_info）の未ログイン取得可否
3. 瀬戸内町営2航路の正確な寄港地名（`routes.yml` に `verified: false` で記載）
4. 気象庁の警報コードと警報名の対応表
5. Blogger API のOAuthリフレッシュトークン取得

## 8. 配船予定と「現在の運航」

### 8.1 なぜ必要か

船のドック入りと便の運休は別物である。ドック中の船の便は別の船が担うため、
「フェリー波之上はドック入り」だけを示すと、その日に鹿児島から船が出ないと
読まれてしまう。日付ごとにどの船が動くかを示して初めて、両者を区別できる。

### 8.2 分かったこと

鹿児島〜奄美群島〜沖縄は、マルエーフェリー2隻（あけぼの・波之上）と
マリックスライン2隻（クロス・プラス）が **1日1便を4隻で分け合う**。
両社の予定を重ねると、月の全日がちょうど1回ずつ埋まる。

1隻の周期は、鹿児島発（D日18:00）→ 那覇着（D+1日19:00）→
那覇発（D+2日07:00）→ 鹿児島着（D+3日08:30）→ 次の鹿児島発（D+3日18:00）。

### 8.3 情報源

いずれも運航会社自身が公表しているものだけを使う。

| 用途 | 会社 | 情報源 | 形式 |
|---|---|---|---|
| 配船（下り・上り） | マルエーフェリー | 年間スケジュール（鹿児島航路） | PDF（記号表） |
| 配船（下り・上り） | マリックスライン | 時刻表と運賃を検索 | 日付と区間を送って結果を得る |
| 時刻 | マルエーフェリー | 乗り場とアクセス | HTMLの表 |
| 時刻 | マリックスライン | 年間運航スケジュール | PDF |

時刻は両社が同じものを掲げており、下り・上りとも一致することを確認した。
マルエーの時刻表のページはPDFの配布だけで、時刻そのものは「乗り場とアクセス」に載っている。

マルエーのPDFは、文字を順に取り出すと空欄が失われて日付とずれる
（30日の表に記号が23個しか並ばない）。座標を保ったまま日付の見出しへ
割り当てて解決した。

マリックスの検索は、運航日には時刻表と「運航予定の船」を返し、
運航しない日は何も返さない。相手先への負荷を避けるため、
呼ぶのは12時間に1回・必要な日数だけとする。

**A"LINEの「乗船検索」は使わない。** 日付を変えても同じ表を返し、
実在する便を「出港船なし」と答えるため、情報源にできない。

**第一海運の配船予定表は読者に案内しない。** 運航会社自身の発表ではないため、
内部の突き合わせにのみ使う。

### 8.4 検査

PDFの解析はレイアウトが変われば静かに壊れ、もっともらしい数字を出し続ける。
誤った予定を示すのは示さないより有害なので、両社を重ねて検査する。

- **同じ日に2隻が鹿児島を出港** → 必ず解析の異常。表示しない。
- **出港の無い日が月に何日もある** → 列がずれた兆候。表示しない。
- **出港の無い日が1日だけ** → ドックの端境期。実際に起こる（2026年10月28日）。
- **下りの2日後の上りが別の船** → 周期が崩れている。表示しない。

最後の検査は、下りと上りを別々に読み取っていることを利用する。
1隻は鹿児島をD日18:00に出て翌日19:00に那覇へ着き、D+2日07:00に那覇を出る。
したがって、ある日の下りの船は2日後の上りの船と同じでなければならない。
2つの読み取りが互いの検算になる。

日次の点検では、配船予定が26時間以内に取り直されているか、
明日まで届いているかも確かめる。予定は静かに古くなるため。

### 8.4.1 奄美海運

この航路は事情が違う。フェリーあまみが機関故障で運休中で、
フェリーきかいが週3便の臨時体制で走っている。通常の年間配船表を使うと、
走っていない船を走っていることにしてしまうため、公式の
「フェリーきかい週3便運航のご案内」だけを見る。

月・水・金に鹿児島北埠頭を17:30に出て、翌日12:20に平土野へ着き、
同じ日の12:50に折り返して、翌々日08:30に鹿児島へ戻る。
鹿児島の乗り場は北埠頭で、マルエー・マリックスの鹿児島新港とは別。
知名港行きは令和7年6月より休止している。

相手が1社しかないため、会社どうしの突き合わせができない。
代わりに、案内そのものが前提どおりかを毎回確かめる。

- **フェリーきかいが月・水・金の列に入っているか**
- **想定していない船が運航する列に入っていないか**（あまみの復帰を見落とさないため）
- **持っている時刻がすべて案内に載っているか**
- **下りの出港日の翌日が上りの出港日になっているか**

ひとつでも外れたら体制が変わったということなので、何も表示しない。

### 8.4.2 台風ページの運航の発表

台風のページで知りたいのは「この数日、船は出るのか」であって、
2か月先のドック予定ではない。期間を今日から3日に絞る。

並べる軸は航路ではなく日付にする。台風のときは日付が主語になるため、
「今日の便はどうか」を探しやすい。1行に船名・状態・区間・船社を置く。

船に関する案内（ドック入り・機関故障）は日付の並びに混ぜず、
「船の状況」としてまとめる。船が離れても別の船が同じ便を担うため、
同じ並びに置くと「その日は船が無い」と読まれてしまう。

節の並びは、台風の現況 → 警報 → 欠航の発表 → 進路予想とする。
進路予想には見方の説明が長く続くため、その手前に行動に関わるものを置く。
発表があるときはページ先頭からそこへ飛べるようにする。

### 8.5 表示

「運航状況」ページの先頭に「現在の運航」を置き、下り便・上り便それぞれに
**航行中**（出港済みで終点未着）と**次の出港**を示す。

便は25時間かかって毎日出るため、同じ向きで2隻が同時に航行している時間帯がある。
その場合は両方を示す。

途中の寄港地は示さない。示すのは出発港と到着港、その時刻、船名だけにする。

貨物船は、公式ページへのリンクだけを置く。発表の形式が各社まちまちで、
自動で追えるところまで確かめきれていない。中途半端に自動化して取りこぼすより、
公式へ案内するほうが確かなので、「公式ページで確認してください」とだけ伝える。

このページは動いている船を示すもので、発表の一覧は持たない。
欠航・条件付運航・ドック入りの一覧は航路ごとの常設ページにあり、
このページからは案内のリンクだけを置く。案内が無いと
「発表が無い」と読まれるため、リンクは必ず出す。
